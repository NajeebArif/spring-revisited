#specify the app name should match with the folder name in the Cloud Config Server
spring.application.name=appgateway

#specify the active profile to use (default, dev)
spring.profiles.active=default

#specify the spring cloud config server url
spring.cloud.config.uri=http://localhost:9999/ConfigServer